services:
  my-http-app:
    build: .
    container_name: my-http-app
    restart: unless-stopped
    environment:
      - PORT=3000
      - TASK_ORG_DATA_DIR=/app/data
    ports:
      - "192.168.88.50:3000:3000"   # change to your server LAN IP
    volumes:
      - ./data:/app/data
